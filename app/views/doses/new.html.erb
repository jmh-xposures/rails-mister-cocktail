<h1>Add ingredients & Dose</h1>
<p></p>

<div class="container">
    <%= simple_form_for([ @cocktail, @dose ])do |f| %>
      <%= f.input :description %>
      <%= f.association :ingredient %>
      <%= f.button :submit, class: "btn" %>
      <% end %>
</div>
